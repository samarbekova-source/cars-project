import React from "react";
import AddCars from "../AddCars/AddCars";
import CarsList from "../CarsList/CarsList";

const Home = () => {
  return (
    <div>
      <AddCars />
      <CarsList />
    </div>
  );
};

export default Home;
